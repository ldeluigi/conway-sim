package core.campaign;

/**
 * These constants define whether or not a cell on the screen should be editable
 * by the user, preventing level cheating.
 */
public enum Editable {
    /**
     * If should be editable.
     */
    EDITABLE,
    /**
     * If shouldn't be editable.
     */
    UNEDITABLE;
}
